#include<iostream>
#include<algorithm>

using namespace std;

long long dp[1001][101];
int main() {
	int k,n;
	cin>>k>>n;
	vector<int> v(n);
	for(int i=0;i<n;i++)cin>>v[i];
	sort(v.begin(), v.end());

	for(int i=1;i<=n;i++) {
		for(int m=1;m<=k;m++) {
			d[i][m]=min()
		}
	}
}
